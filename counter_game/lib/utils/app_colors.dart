import 'package:flutter/material.dart';

class AppColors {
  // PRIMARY COLORS
  static const Color primary = Color(0xFFE51B7A);
  static const Color primaryLight = Color(0xFFFF5FA2);
  static const Color primaryDark = Color(0xFFC91468);

  // BACKGROUND
  // Background utama aplikasi
  static const Color background = Color(0xFFFFF7FA);

  // Background/card putih
  static const Color surface = Color(0xFFFFFFFF);

  // Background pink sangat muda untuk area tertentu
  static const Color surfacePink = Color(0xFFFFF0F6);

  // PLAYER COLORS
  static const Color player1 = Color(0xFFE51B7A);
  static const Color player2 = Color(0xFFF08BAF);

  // TEXT COLORS
  // Judul dan teks utama
  static const Color textPrimary = Color(0xFF25232A);

  // Teks sekunder
  static const Color textSecondary = Color(0xFF77717A);

  // Teks putih di atas tombol pink
  static const Color textOnPrimary = Color(0xFFFFFFFF);

  // BORDER
  // Border card dan button
  static const Color border = Color(0xFFF1C9DA);

  // Border yang lebih soft
  static const Color borderLight = Color(0xFFF8DDE9);

  // GAME STATUS
  // Warna ketika pemain menang
  static const Color winner = Color(0xFFE51B7A);

  // Warna untuk status serving
  static const Color serving = Color(0xFFE51B7A);

  // Warna untuk status defending
  static const Color defending = Color(0xFFF08BAF);

  // Tombol yang sudah tidak bisa digunakan
  static const Color disabled = Color(0xFFE5DDE1);

  // Teks pada tombol disabled
  static const Color disabledText = Color(0xFFAAA2A7);

  // ICON
  static const Color iconPrimary = Color(0xFFE51B7A);
  static const Color iconSecondary = Color(0xFF77717A);

  // DIVIDER
  static const Color divider = Color(0xFFF3E2E9);
}
